The dossier ID needs to be in following format:
"dossier-{sequence number}"


    >>> from plone.dexterity.utils import createContentInContainer
    >>> from opengever.base.interfaces import ISequenceNumber
    >>> from zope.component import getUtility

Create a dossier:

	>>> portal = layer['portal']
    >>> d1 = createContentInContainer(portal,
    ...                               'opengever.dossier.businesscasedossier')
    >>> d1
    <DossierContainer at /plone/dossier-3>
    >>> d1.id
    'dossier-3'
    >>> getUtility(ISequenceNumber).get_number(d1)
    3

Create another dossier:

    >>> d2 = createContentInContainer(portal,
    ...                               'opengever.dossier.businesscasedossier')
    >>> d2
    <DossierContainer at /plone/dossier-4>
    >>> d2.id
    'dossier-4'
    >>> getUtility(ISequenceNumber).get_number(d2)
    4

