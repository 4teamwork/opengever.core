Create a user

    >>> from opengever.ogds.base.model.user import User
    >>> user = User('hugo.boss')
    >>> user
    <User hugo.boss>
    >>> session.add(user)

Get the user

    >>> session.query(User).one()
    <User hugo.boss>
